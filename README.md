# Turing-Machine-Simulation-Java
Java implementation of a Turing Machine (TM) simulator. This project models the TM structure (states, tape, transitions) and simulates its execution to recognize formal languages.
